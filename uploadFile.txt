gross deduc-->reports -->locwise gross net-->not asking for locationCPF.csv upload-->chanage from dat to csv57750declare	dedn_amt_sum number:=0;	linebuf varchar2(2000);	infile  client_Text_IO.File_Type;	ln_sln number :=1;	cursor c is SELECT emp_code empcode,emp_name empname,emp_level_code emplevel,ed_code earcode,ed_desc eardesc,act_ed_amount earamount FROM actualgrossview,ed_param,EMPmstview WHERE act_ed_code=ed_code AND act_emp_code=emp_code AND emp_leaving_date IS NULL ORDER BY 1,4;begin	set_application_property(cursor_style,'BUSY');	infile := client_Text_IO.Fopen('C:\PAYROLL\SPOOL\'||UPPER('Actual_Gross_Details')||'.CSV','W');	client_text_io.put_line(infile,',,,,,TITAN  COMPANY  LIMITED' );	client_text_io.put_line(infile,',,,,,Actual Gross Details Report' );	client_text_io.put_line(infile,'' );	client_text_io.put_line(infile,'EMPCODE,EMPNAME,EMPLEVEL,EARCODE,EARDESC,EARAMOUNT' );	client_text_io.put_line(infile,'' );	for i in c loop	  client_text_io.put_line(infile,I.EMPCODE||','|| i.EMPNAME||','||i.EMPLEVEL||','||i.EARCODE||','||i.EARDESC||','||i.EARAMOUNT); 	  ln_sln := ln_sln + 1;	   if mod(ln_sln,10000) = 0 then synchronize;     end if;	end loop; 	client_text_io.fclose(infile);  set_application_property(cursor_style,'DEFAULT');	Exception		when others then		message(sqlerrm);	  end;declarepat varchar2(100);beginuser_name;pat:='plus33w -s '||:GLOBAL.TEXT||' @i:\sqls\locwise_gross_ded.sql';client_host(pat);end;